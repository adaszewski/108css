let tekstPierwszy = "Uwielbiam JavaScript";
let tekstDrugi = "Jestem świetnym programistą"
function dluzszyTekst(pierwszy, drugi) {
    if (length.pierwszy > length.drugi) {
        return pierwszy
    } else {
        return drugi
    }
}
console.log(dluzszyTekst(tekstPierwszy, tekstDrugi));